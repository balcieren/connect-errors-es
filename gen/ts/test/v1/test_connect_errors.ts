// @generated by protoc-gen-connect-errors-es v0.1.0 with parameter "target=ts"
// @generated from file test/v1/test.proto (package test.v1, syntax proto3)
/* eslint-disable */

import { create, extractErrorCode, registerAll } from "connect-errors";
import { Code, ConnectError } from "@connectrpc/connect";

// ── Error code constants ────────────────────────────
export const ErrUserNotFound = "ERROR_USER_NOT_FOUND" as const;

// ── Auto-register ───────────────────────────────────
registerAll([
  {
    code: ErrUserNotFound,
    messageTpl: "User with ID {{id}} not found in database {{db}}",
    connectCode: Code.NotFound,
    retryable: false,
  },
]);

// ── Typed constructors ──────────────────────────────

export interface UserNotFoundParams {
  id: string;
  db: string;
}

export function newUserNotFound(p: UserNotFoundParams): ConnectError {
  return create(ErrUserNotFound, {
    id: p.id,
    db: p.db,
  });
}

// ── Client-side error matchers ──────────────────────

export function isUserNotFound(err: unknown): boolean {
  if (!(err instanceof ConnectError)) return false;
  return extractErrorCode(err) === ErrUserNotFound;
}
